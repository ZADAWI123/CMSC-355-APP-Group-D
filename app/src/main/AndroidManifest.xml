<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cmsc355app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WarningActivity"></activity>
        <activity android:name=".challenges" />
        <activity android:name=".help" />
        <activity android:name=".Goals" />
        <activity android:name=".workExamples" />
        <activity android:name=".SelectionTab" />
        <activity android:name=".main_hub" />
        <activity android:name=".workout_tab" />
        <activity android:name=".Progression_Tab" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>