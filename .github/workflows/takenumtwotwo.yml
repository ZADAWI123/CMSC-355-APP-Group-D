name: Push on commit

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Send Push Notification
      uses: techulus/push-github-action@1.0.0
      env:
        API_KEY: ${{ secrets.276bc872-9c1f-4251-85ea-e7706adf1c00 }}
        MESSAGE: "There is a new commit!"
